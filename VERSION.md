# Version 1.2.0 — 2026-08-30

Release tag: `v1.2.0`

- 原生导出并解析 `strategy-flow-input/0.2`。
- 新增 `strategy-taxonomy/2026-09` 标签字典、结构化标签选择和三类父子级联校验。
- 策略基础信息移除业务场景 / 策略类型 / 策略子类，统一收敛到 `taxonomy`。
- 新增自定义标签提案模型；code 由 Workbench 审批生成，未审批提案保留 warning。
- 新增 `strategy-flow-registration-metadata/2.0` companion 导入 / 导出。
- 兼容导入 `strategy-flow-input/0.1`，显式升级为 0.2 并提示缺失标签。
- 解析器改为显式版本注册表；未知未来版本拒绝而不是假兼容。
- 编辑器顶栏、右侧四 Tab 属性面板、策略标签分组、校验定位、代码子 Tab 和导出抽屉完成信息架构重构。
- 第二轮 Fluent UI 优化：移除范式图标装饰，增强校验信息密度，支持三栏自由拉伸，并统一中英文字体与交互反馈。
- 信息架构按访问频率重排：右侧属性栏专注高频当前对象编辑，基础信息 / 策略标签 / 元数据收进顶部“本策略注册信息”抽屉。

# Version 1.1.0

- 新增流程卡片与流转规则复制、粘贴，支持 Shift 框选和批量操作。
- 流转规则执行人和对象两侧行为新增“无动作”；导出时固定为 `no_requirement` + `无动作`。
- 自动布局改为按流程依赖分层，减少同类卡片简单堆叠。
- 新增撤销 / 重做；双向规则增加双向引导视觉标识。
- 流程卡片执行人、对象、对象状态改为同优先级展示，并补充“进入条件”业务解释。

# Version 1.0.0

- 项目升级为 1.0.0。
- 新增 npm package 元数据和 `strategy-flow-input` CLI。
- 支持 `npm install github:zhengxiaonu-creator/strategy-flow-input` 后本地启动设计器。

# Version 0.13.0

- 双击流程卡片或流转规则标签时自动展开右侧属性栏并进入对应编辑态。

# Version 0.12.0

- 初始逻辑画布扩大到 4800 × 3200。
- 画布最小缩放比例降低到 25%。
- 流转曲线 `normalOffset` 范围扩大到 -4800 ~ 4800。

# Version 0.11.0

- 同源出边和同目标入边恢复为共享卡片锚点。
- 平行曲线通过控制点法线偏移分层，并支持拖拽流转规则标签调整间距。
- 流转规则标签沿各自曲线中点法线方向放置，并自动避让重叠。

# Version 0.10.0

- 流转规则文案统一从“主体”改为“对象”，卡片状态改为“对象状态”。
- 流转规则标签完整展示执行人和对象的时间、状态、行为。
- 一对多连线按来源和目标锚点分层，连线与标签不再重叠。

# Version 0.9.1

- 修复从右侧属性栏按住拖出时，浏览器派发背景 click 导致编辑状态丢失的问题。

# Version 0.9.0

- 对象拆分为对象类型、对象名称、当前状态三要素。
- Schema、页面、通用示例和 E2E 同步支持 `subject.name`。

# Version 0.8.0

- 流程卡片将“对象”和“当前状态”拆开展示与编辑。
- 增加对象、当前状态的业务解释，并同步更新通用示例与 Schema 描述。

# Version 0.7.0

- 新增「保存草稿」按钮和自动保存状态提示。
- 修复流程卡片嵌套字段、流转规则执行动作 / 对象行为修改后未能写入草稿的问题。

# Version 0.6.0

- 画布新增放大、缩小、重置缩放和 Ctrl/⌘ + 滚轮缩放。
- 缩放以鼠标位置为中心，视觉缩放不改变流程卡片布局坐标。

# Version 0.5.1

- 修复底部代码栏标题行错误占用弹性空间的问题，输出区域恢复为主要区域。

# Version 0.5.0

- 采用 Fluent Light 视觉语言重构命令栏、Mica 侧栏、Acrylic 画布卡片和底部 Dock。
- 统一 Fluent 色板、圆角、阴影、焦点环与动效曲线；保留 EBSCN 品牌标识。

# Version 0.4.0

- 面向业务人员重命名流程卡片、客户触达内容和执行跟进动作，并补充业务解释。
- 内部卡片、流转规则、动作编号自动生成，业务人员不再手工填写或修改。
- 导入 JSON 缺少内部编号时自动补齐；显式非法编号仍保留并进入校验错误。

# Version 0.3.0

- 编排器定位为全项目通用工具，移除具体策略专属示例内容和固定时间枚举。
- 时间 / 阶段改为自由业务表达式，由各策略范式适配器解释排序和口径。

# Version 0.2.0

- 新增左栏、右栏、下栏收起 / 展开控制。
- 面板显示状态保存到本地，刷新后保持用户布局。

# Version 0.1.0

- 首个静态 MVP：策略编排画布、结构化 JSON、Mermaid 导出、实时校验与本地草稿。
- 契约版本：`strategy-flow-input/0.1`。
- 本版本不写工单、不写 registry、不直接生成提交表。
